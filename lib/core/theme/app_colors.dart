import 'package:flutter/material.dart';

/// AURA color palette — dark mode first.
class AppColors {
  const AppColors._();

  // Brand
  static const Color primary = Color(0xFF5B8CFF);
  static const Color accent = Color(0xFF6EE7F9);

  // Dark surfaces
  static const Color background = Color(0xFF0D1117);
  static const Color card = Color(0xFF161B22);
  static const Color cardAlt = Color(0xFF1C2330);
  static const Color border = Color(0xFF262D38);
  static const Color textPrimary = Color(0xFFE8ECF2);
  static const Color textSecondary = Color(0xFF8B95A5);

  // Light surfaces
  static const Color lightBackground = Color(0xFFF6F8FB);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightCardAlt = Color(0xFFEFF2F7);
  static const Color lightBorder = Color(0xFFE2E8F0);
  static const Color lightTextPrimary = Color(0xFF0D1117);
  static const Color lightTextSecondary = Color(0xFF5A6472);

  // Semantic
  static const Color success = Color(0xFF4ADE80);
  static const Color warning = Color(0xFFFBBF24);
  static const Color error = Color(0xFFF87171);
}
